package linked_lists.check_palindrome;

public class CheckPalindromeTest {
    Node head;

    static class Node{
        int data;
        Node next;
        Node(int data){
            this.data = data;
            next = null;
        }
    }

    public boolean checkPalindrome(){

        return false;
    }
}
